apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: el-starterkit-listener
spec:
  tls:
    termination: edge
  to:
    kind: Service
    name: el-starterkit-listener
    weight: 100
  port:
    targetPort: http-listener
  wildcardPolicy: None
