apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # pipelines
  - pipeline.yaml
  # tasks
  - sonarqube.yaml
  - git-cli.yaml
  - git-clone.yaml
  - kustomize.yaml
  - trivy.yaml
  # preparations
  #- tekton-pvc.yaml
  - trigger-sa.yaml
  - ${{values.app_name}}-eventlistener.yaml
  - ${{values.app_name}}-triggerbinding.yaml
  - ${{values.app_name}}-triggertemplate.yaml
  - el-route.yaml 
