apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # pipelines
  - pipeline.yaml
  # tasks
  - sonarqube.yaml
  - git-cli.yaml
  - git-clone.yaml
  - kustomize.yaml
  - trivy.yaml
  # preparations
  - tekton-pvc.yaml
  - torigger-sa.yaml
  - health-eventlistner.yaml
  - health-triggerbinding.yaml
  - health-triggertemplate.yaml
  - el-route.yaml 
